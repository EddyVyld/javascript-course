/* import React, { useState } from 'react';

export const ClickableContact = () => {
    const [name, setName] = useState('Juan');

    const handleClick = () => {
        alert('Esto es la página de Contáctanos');
    };

    return (
        <div>
            <h1>{name}</h1>
            <button onClick={handleClick}>Click me</button>
        </div>
    );
}; */
